# TYPE  DATABASE  USER  ADDRESS          METHOD
# Conexiones por socket Unix: sin contraseña (igual que instalación local)
local   all       all                    trust
# Conexiones TCP/IP dentro de la red Docker interna: sin contraseña
# La red pgnet es privada; el proxy de Codespaces ya exige autenticación externa.
# Esto replica el comportamiento de una instalación local para los ejercicios.
host    all       all   0.0.0.0/0        trust
